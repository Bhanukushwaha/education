<!-- <p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Address</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @users.each do |user| %>
      <tr>
        <td><%#= user.first_name %></td>
        <td><%#= user.last_name %></td>
        <td><%#= user.address %></td>
        <td><%#= user.email %></td>
        <td><%#= link_to 'Show', polymorphic_path([:admin, user]) %></td>
        <td><%#= link_to 'Edit', edit_admin_user_path(user) %></td>
        <td><%#= link_to 'Destroy', polymorphic_path([:admin, user]), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <%# end %>
  </tbody>
</table>

<br>

<%#= link_to 'New User', new_admin_user_path %>
 -->


<div class="sb2-2-3">
  <div class="row">
    <div class="col-md-12">
      <div class="box-inn-sp">
        <div class="inn-title">
          <h4>Student Details</h4>
          <p>All about students like name, student id, phone, email, country, city and more</p>
        </div>
        <div class="tab-inn">
          <div class="table-responsive table-desi">
            <table class="table table-hover">
              <thead>
                <tr>
                  <th>User</th>
                  <th>Name</th>
                  <th>Phone</th>
                  <th>Email</th>
                  <th>Country</th>
                  <th>Id</th>
                  <th>Date of birth</th>
                  <th>Status</th>
                  <th>View</th>
                </tr>
              </thead>
              <tbody>
                <% @users.each do |user| %>
                  <tr>
                    <% if user.image.present? %>
                      <td><span class="list-img"><img src="<%= user.image %>" alt=""></span>
                    </td>
                    <% else %>
                      <td><span class="list-img"><img src="<%= asset_path("user/1.png")%>" alt=""></span>
                    </td>
                    <% end %>
                    <td><a href="/admin/users/<%= user.id%>" title="Details"><span class="list-enq-name"><%= user.name%></span><span class="list-enq-city"><%= user.try(:city)%>, <%= user.try(:country)%>s</span></a>
                    </td>
                    <td>+91<%= user.try(:phone)%> </td>
                    <td><%= user.email%></td>
                    <td><%= user.try(:country)%></td>
                    <td><%= user.try(:student_id)%></td>
                    <td>03 Jun 1990</td>
                    <td>
                      <span class="label label-success">Active</span>
                    </td>
                    <td><a href="/admin/users/<%= user.id%>/edit" class="ad-st-view">View</a></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>